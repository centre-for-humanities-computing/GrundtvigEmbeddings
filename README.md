# GrundtvigEmbeddings

## Requirements

```python
pip install gensim
```

## Querying

To query run 
```
python embedding.py --wordcount [num]
```
Where `num` is an integer of how many words to output.
